/**
 * Quiet Authority Applications workspace: one operational card that uses shared frontend-only Admin records for local management-list interactions.
 */
import { AdminShell } from "@/components/admin/AdminShell";
import { FoundationInput, FoundationSelect, StatusBadge } from "@/components/foundation/ui";
import { Pagination, PaginationContent, PaginationItem, PaginationLink, PaginationNext, PaginationPrevious } from "@/components/ui/pagination";
import { getAdminApplications, type AdminApplication, type AdminStatus, type AssessmentState } from "@/lib/adminMockData";
import { ArrowDown, ArrowUp, ArrowUpDown, Search } from "lucide-react";
import { useMemo, useState } from "react";
import { useLocation } from "wouter";

type SortKey = "candidateName" | "applicationStatus" | "assessmentStatus" | "appliedDate";
type SortDirection = "asc" | "desc";
const PAGE_SIZE = 10;
const applicationStatusOptions: AdminStatus[] = ["In Progress", "Submitted", "Under Review", "Shortlisted", "Interview", "Rejected", "Hired"];
const assessmentStatusOptions: AssessmentState[] = ["Not Started", "In Progress", "Completed"];

function SortControl({ column, label, sortKey, direction, onSort }: { column: SortKey; label: string; sortKey: SortKey; direction: SortDirection; onSort: (column: SortKey) => void }) {
  const Icon = sortKey !== column ? ArrowUpDown : direction === "asc" ? ArrowUp : ArrowDown;
  return <button className="inline-flex items-center gap-1.5 text-[11px] font-semibold uppercase tracking-[0.08em] text-muted-foreground transition-colors hover:text-primary" onClick={() => onSort(column)} type="button">{label}<Icon className="size-3" /></button>;
}

function CandidateCell({ application }: { application: AdminApplication }) {
  return <div><p className="font-medium text-primary">{application.candidateName}</p><p className="mt-1 text-[12px] text-muted-foreground">{application.email}</p></div>;
}

export default function AdminApplications() {
  const [, setLocation] = useLocation();
  const [applications] = useState(() => getAdminApplications());
  const [search, setSearch] = useState("");
  const [role, setRole] = useState("all");
  const [applicationStatus, setApplicationStatus] = useState("all");
  const [assessmentStatus, setAssessmentStatus] = useState("all");
  const [sortKey, setSortKey] = useState<SortKey>("appliedDate");
  const [sortDirection, setSortDirection] = useState<SortDirection>("desc");
  const [page, setPage] = useState(1);
  const normalizedSearch = search.trim().toLowerCase();
  const hasActiveFilters = Boolean(normalizedSearch) || role !== "all" || applicationStatus !== "all" || assessmentStatus !== "all";

  const filteredApplications = useMemo(() => {
    const records = applications.filter((application) => {
      const matchesSearch = !normalizedSearch || application.candidateName.toLowerCase().includes(normalizedSearch) || application.email.toLowerCase().includes(normalizedSearch);
      return matchesSearch && (role === "all" || application.role === role) && (applicationStatus === "all" || application.applicationStatus === applicationStatus) && (assessmentStatus === "all" || application.assessmentStatus === assessmentStatus);
    });
    return [...records].sort((first, second) => {
      const comparison = first[sortKey].localeCompare(second[sortKey]);
      return sortDirection === "asc" ? comparison : -comparison;
    });
  }, [applicationStatus, applications, assessmentStatus, normalizedSearch, role, sortDirection, sortKey]);

  const totalPages = Math.max(1, Math.ceil(filteredApplications.length / PAGE_SIZE));
  const currentPage = Math.min(page, totalPages);
  const visibleApplications = filteredApplications.slice((currentPage - 1) * PAGE_SIZE, currentPage * PAGE_SIZE);
  const sort = (column: SortKey) => { if (column === sortKey) setSortDirection((direction) => direction === "asc" ? "desc" : "asc"); else { setSortKey(column); setSortDirection(column === "appliedDate" ? "desc" : "asc"); } };
  const resetFilters = () => { setSearch(""); setRole("all"); setApplicationStatus("all"); setAssessmentStatus("all"); setPage(1); };
  const updateFilter = (setFilter: (value: string) => void) => (value: string) => { setFilter(value); setPage(1); };

  return <AdminShell title="Applications"><section><p className="text-[12px] font-medium text-muted-foreground">Recruitment management</p><h2 className="mt-2 text-3xl font-semibold tracking-[-0.035em] text-primary">Applications</h2><p className="mt-2 text-[15px] leading-6 text-muted-foreground">Review and manage applications received for current recruitment roles.</p></section>
    <section className="mt-6 grid gap-3 sm:grid-cols-2 xl:grid-cols-4" aria-label="Application summary"><article className="rounded-xl border border-border bg-white p-4"><p className="text-[12px] font-medium text-muted-foreground">Total Applications</p><p className="mt-2 text-2xl font-semibold tracking-[-0.04em] text-primary">24</p><p className="mt-1.5 text-[12px] text-muted-foreground">All applications</p></article><article className="rounded-xl border border-border bg-white p-4"><p className="text-[12px] font-medium text-muted-foreground">Submitted</p><p className="mt-2 text-2xl font-semibold tracking-[-0.04em] text-primary">18</p><p className="mt-1.5 text-[12px] text-muted-foreground">Ready for review</p></article><article className="rounded-xl border border-border bg-white p-4"><p className="text-[12px] font-medium text-muted-foreground">In Progress</p><p className="mt-2 text-2xl font-semibold tracking-[-0.04em] text-primary">6</p><p className="mt-1.5 text-[12px] text-muted-foreground">Not yet submitted</p></article><article className="rounded-xl border border-border bg-white p-4"><p className="text-[12px] font-medium text-muted-foreground">Shortlisted</p><p className="mt-2 text-2xl font-semibold tracking-[-0.04em] text-primary">0</p><p className="mt-1.5 text-[12px] text-muted-foreground">No candidates shortlisted yet</p></article></section>
    <section className="mt-6 rounded-xl border border-border bg-white p-4 sm:p-5 lg:p-6"><div className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div className="relative w-full lg:max-w-sm"><label className="sr-only" htmlFor="application-search">Search by candidate name or email</label><Search className="pointer-events-none absolute left-3.5 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /><FoundationInput id="application-search" onChange={(event) => { setSearch(event.target.value); setPage(1); }} placeholder="Search by candidate name or email" value={search} className="pl-10" /></div><div className="grid grid-cols-1 gap-3 sm:grid-cols-3 lg:w-auto"><div><label className="mb-1.5 block text-[12px] font-medium text-muted-foreground" htmlFor="role-filter">Role</label><FoundationSelect id="role-filter" onChange={(event) => updateFilter(setRole)(event.target.value)} value={role}><option value="all">All roles</option><option value="Business Development Manager">Business Development Manager</option></FoundationSelect></div><div><label className="mb-1.5 block text-[12px] font-medium text-muted-foreground" htmlFor="application-status-filter">Application status</label><FoundationSelect id="application-status-filter" onChange={(event) => updateFilter(setApplicationStatus)(event.target.value)} value={applicationStatus}><option value="all">All statuses</option>{applicationStatusOptions.map((status) => <option key={status} value={status}>{status}</option>)}</FoundationSelect></div><div><label className="mb-1.5 block text-[12px] font-medium text-muted-foreground" htmlFor="assessment-status-filter">Assessment</label><FoundationSelect id="assessment-status-filter" onChange={(event) => updateFilter(setAssessmentStatus)(event.target.value)} value={assessmentStatus}><option value="all">All assessment statuses</option>{assessmentStatusOptions.map((status) => <option key={status} value={status}>{status}</option>)}</FoundationSelect></div></div></div>
      <div className="mt-5 flex items-center justify-between gap-4 border-t border-border pt-4"><p className="text-[13px] text-muted-foreground">{filteredApplications.length} {filteredApplications.length === 1 ? "application" : "applications"}</p>{hasActiveFilters ? <button className="text-[13px] font-medium text-portal-blue transition-colors hover:text-primary hover:underline" onClick={resetFilters} type="button">Clear filters</button> : null}</div>
      {visibleApplications.length ? <><div className="mt-3 hidden overflow-x-auto md:block"><table className="w-full min-w-[950px] text-left"><thead className="border-b border-border"><tr><th className="px-3 py-3"><SortControl column="candidateName" direction={sortDirection} label="Candidate" onSort={sort} sortKey={sortKey} /></th><th className="px-3 py-3 text-[11px] font-semibold uppercase tracking-[0.08em] text-muted-foreground">Role</th><th className="px-3 py-3"><SortControl column="applicationStatus" direction={sortDirection} label="Application Status" onSort={sort} sortKey={sortKey} /></th><th className="px-3 py-3"><SortControl column="assessmentStatus" direction={sortDirection} label="Assessment" onSort={sort} sortKey={sortKey} /></th><th className="px-3 py-3"><SortControl column="appliedDate" direction={sortDirection} label="Applied" onSort={sort} sortKey={sortKey} /></th><th className="px-3 py-3 text-right text-[11px] font-semibold uppercase tracking-[0.08em] text-muted-foreground">Action</th></tr></thead><tbody className="divide-y divide-border">{visibleApplications.map((application) => <tr key={application.id}><td className="px-3 py-4 text-sm"><CandidateCell application={application} /></td><td className="max-w-[170px] px-3 py-4 text-[13px] leading-5 text-muted-foreground">{application.role}</td><td className="px-3 py-4"><StatusBadge status={application.applicationStatus} /></td><td className="px-3 py-4"><StatusBadge status={application.assessmentStatus} /></td><td className="whitespace-nowrap px-3 py-4 text-[13px] text-muted-foreground">{application.appliedDateLabel}</td><td className="px-3 py-4 text-right"><button className="text-[13px] font-medium text-portal-blue transition-colors hover:text-primary hover:underline" onClick={() => setLocation(`/admin/applications/${application.id}`)} type="button">View</button></td></tr>)}</tbody></table></div><div className="mt-3 divide-y divide-border md:hidden">{visibleApplications.map((application) => <article className="py-4" key={application.id}><CandidateCell application={application} /><div className="mt-4 grid grid-cols-2 gap-x-4 gap-y-4"><div><p className="text-[11px] font-semibold uppercase tracking-[0.08em] text-muted-foreground">Role</p><p className="mt-1 text-[13px] leading-5 text-primary">{application.role}</p></div><div><p className="text-[11px] font-semibold uppercase tracking-[0.08em] text-muted-foreground">Application status</p><div className="mt-1.5"><StatusBadge status={application.applicationStatus} /></div></div><div><p className="text-[11px] font-semibold uppercase tracking-[0.08em] text-muted-foreground">Assessment</p><div className="mt-1.5"><StatusBadge status={application.assessmentStatus} /></div></div><div><p className="text-[11px] font-semibold uppercase tracking-[0.08em] text-muted-foreground">Applied</p><p className="mt-1 text-[13px] text-primary">{application.appliedDateLabel}</p></div></div><button className="mt-4 text-[13px] font-medium text-portal-blue transition-colors hover:text-primary hover:underline" onClick={() => setLocation(`/admin/applications/${application.id}`)} type="button">View application</button></article>)}</div><Pagination className="mt-6 justify-end border-t border-border pt-4"><PaginationContent><PaginationItem><PaginationPrevious aria-disabled={currentPage === 1} className={currentPage === 1 ? "pointer-events-none opacity-45" : undefined} href="#applications" onClick={(event) => { event.preventDefault(); if (currentPage > 1) setPage(currentPage - 1); }} /></PaginationItem>{Array.from({ length: totalPages }, (_, index) => index + 1).map((pageNumber) => <PaginationItem key={pageNumber}><PaginationLink href="#applications" isActive={pageNumber === currentPage} onClick={(event) => { event.preventDefault(); setPage(pageNumber); }}>{pageNumber}</PaginationLink></PaginationItem>)}<PaginationItem><PaginationNext aria-disabled={currentPage === totalPages} className={currentPage === totalPages ? "pointer-events-none opacity-45" : undefined} href="#applications" onClick={(event) => { event.preventDefault(); if (currentPage < totalPages) setPage(currentPage + 1); }} /></PaginationItem></PaginationContent></Pagination></> : <div className="py-14 text-center"><h3 className="text-base font-semibold text-primary">No applications found</h3><p className="mt-2 text-sm text-muted-foreground">Try changing your search or filters.</p><button className="mt-4 text-[13px] font-medium text-portal-blue transition-colors hover:text-primary hover:underline" onClick={resetFilters} type="button">Clear filters</button></div>}</section>
  </AdminShell>;
}
