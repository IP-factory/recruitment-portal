/**
 * Quiet Authority portal route: visual-only workspace frame without dashboard, applicant, or admin functionality.
 */
import { PortalShell } from "@/components/foundation/portal-shell";

export default function Portal() {
  return <PortalShell />;
}
